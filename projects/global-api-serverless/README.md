# Global API – Serverless with Lambda + Global Accelerator

This project creates a globally-available serverless API:
- API Gateway + Lambda integration
- Route 53 + Global Accelerator for low-latency access
- S3/CloudFront for static asset delivery
- CloudWatch logging

